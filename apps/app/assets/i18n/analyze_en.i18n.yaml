analyzePage:
  title: Review
  purchaseRate: Purchase Rate
  purchased: Purchased
  format:
    fraction: "${molecule:int}/${denominator:int}"
    month: "Month ${month:int}"
  totalPrice: Total Price
