name: infrastructure_mock
description: "This package provides core features of app."
publish_to: "none"
version: 0.0.1

environment:
  sdk: ^3.10.1

dependencies:
  packages_core:
    path: ../../core
  packages_domain:
    path: ../../domain

dev_dependencies:
  build_runner: ^2.10.4
  flutter_lints: ^6.0.0
  nilts: ^0.25.1

resolution: workspace
